// Window controls (minimize / maximize / close) are removed for the TV-first
// layout, the app is driven by remote/D-pad and Quit lives in the Options page.
export function WindowControls() {
  return null;
}
